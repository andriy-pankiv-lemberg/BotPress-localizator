version: '3'
services:
  translations-db:
    build: db
    container_name: translations-db
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - translations-db-data:/var/lib/postgresql/data

volumes:
  translations-db-data: